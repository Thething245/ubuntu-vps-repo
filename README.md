# Ubuntu SSH VPS - ubuntu-vps-repo

## 🌏 VPS Information

- **OS**: Ubuntu 22.04 LTS
- **Region**: Singapore (Asia-Pacific)
- **Access**: SSH via Ngrok Tunnel
- **Username**: vpsuser
- **Password**: vps123456
- **Runtime**: ~5.5 hours with auto-restart
- **Backup**: Hourly automatic backups

## 📝 How to Connect

1. Wait 2-3 minutes for VPS setup
2. Check **ssh-info.txt** file in this repository
3. Copy the SSH command
4. Run it in your terminal:

```bash
ssh vpsuser@<ngrok-host> -p <ngrok-port>
# Password: vps123456
```

## 📦 Backup System

- **Automatic backups** every hour
- **Backup location**: `.backup/` folder
- **Retention**: Last 5 backups kept
- **Final backup** created before each restart
- **Contains**: SSH info, logs, and configuration files

### Restore from Backup

```bash
# Download backup file from .backup/ folder
tar -xzf backup_YYYYMMDD_HHMMSS.tar.gz
```

## ⚡ Features

- ✅ Ubuntu 22.04 LTS
- ✅ Full sudo access
- ✅ Pre-installed: wget, curl, git, vim, zip
- ✅ Ngrok tunnel for public access
- ✅ Auto-restart every 5.5 hours
- ✅ Hourly automatic backups
- ✅ Singapore region (low latency for Asia)

## 🛠️ Files

- `.github/workflows/ubuntu-ssh.yml`: Main workflow with backup
- `ssh-info.txt`: SSH connection details
- `.backup/`: Backup files (updated hourly)

## ⚠️ Important Notes

- VPS will auto-restart every ~5.5 hours
- Backups are created hourly and committed to repo
- No persistent storage beyond repo commits
- For testing/development only
- Change default password after first login

## 🔒 Security

```bash
# Change password after first login
passwd

# Add SSH key for better security
mkdir -p ~/.ssh
echo "your-public-key" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

---
*Generated by Auto VPS Manager | Backup System Enabled*
